# 第二週作業：測試怎麼寫、怎麼跑

這份作業的 **測試已經寫好了**，檔案就是 `test.js`。  
你不用自己從零寫測試，只要把 `index.js` 的六個函式寫完，再跑 `npm test` 就好。

本文說明：測試是怎麼寫的、怎麼跑、你要對應做什麼。

---

## 先搞清楚兩種「測試」

這份作業其實有兩種驗收方式：

| 方式 | 指令 | 用途 |
|---|---|---|
| 自動測試（繳交一定要過） | `npm test` | Jest 自動檢查 14 項 |
| 手動驗證（自己練習） | `npm start` + curl / Postman | 真的打一支 API 看看上傳成不成功 |

繳交前必須看到：`Tests: 14 passed, 14 total`。

---

## 完整步驟：從零到跑測試

### 第 1 步：安裝套件

在專案資料夾開終端機：

```bash
npm install
```

這會裝好：

- `jest`：測試框架（寫 `describe` / `test` / `expect`）
- `supertest`：假裝送 HTTP 請求，測 server（任務六用）
- `formidable`、`dotenv`：作業本身會用到

`package.json` 已經寫好測試指令：

```json
"test": "jest --runInBand"
```

所以之後只要打 `npm test`，Jest 會自動找到 `test.js` 並執行。  
`--runInBand` 代表一次跑一個測試，避免同時改環境變數互相干擾。

---

### 第 2 步：測試檔開頭在做什麼

`test.js` 開頭大致是：

1. 引入工具：`supertest`、`path`、`os`、`fs`
2. 從 `index.js` **引入你要測的函式**
3. 指定測試用圖片：`fixtures/sample.jpg`

重點：測試不是測 `app.js`，而是直接測 `index.js` 匯出的函式。  
所以你寫作業時，`index.js` 底部一定要有 `module.exports`（作業已經幫你寫好了）。

---

### 第 3 步：Jest 的三種基本寫法

所有測試都長這樣：

```js
describe('這組測什麼', () => {
  test('這筆測什麼', () => {
    const 結果 = 你的函式(參數);
    expect(結果).toBe(期望值);
  });
});
```

白話對照：

- `describe`：資料夾，把同類測試放一起
- `test`：一筆考題
- `expect(...).toBe(...)`：斷言，意思是「我預期結果要等於這個」

常見斷言：

| 寫法 | 意思 |
|---|---|
| `toBe(值)` | 完全相等 |
| `toContain('字')` | 字串裡有這段文字 |
| `toBeDefined()` | 有值、不是 `undefined` |
| `toBeGreaterThan(0)` | 大於 0 |

---

### 第 4 步：任務一～四是「單元測試」（直接呼叫函式）

這類測試最單純：**給輸入 → 看輸出**。你不用開 server。

#### 任務一 `getUploadConfig`

測試會先清掉環境變數，再自己設值，避免你電腦上的 `.env` 影響結果。

測兩種情況：

1. **env 都有設**
   - `UPLOAD_DIR=/tmp/test-upload`
   - `MAX_FILE_SIZE_MB=10`
   - `GYM_NAME=FitClub`
   - 預期：
     - `uploadDir` → `'/tmp/test-upload'`
     - `maxFileSize` → `10 * 1024 * 1024`
     - `gymName` → `'FitClub'`

2. **env 都沒設**
   - 預期：
     - `uploadDir` → `'/tmp'`
     - `maxFileSize` → `5 * 1024 * 1024`
     - `gymName` → `'未命名健身房'`

你要寫的對應邏輯：

- 有設 env → 用 env
- 沒設 → 用上面的預設值
- `MAX_FILE_SIZE_MB` 是字串，要先 `Number()`，再乘 `1024 * 1024` 換成 bytes

#### 任務二 `getFileExtension`

測 4 種檔名：

| 輸入 | 預期 |
|---|---|
| `'cat.jpg'` | `'.jpg'` |
| `'PHOTO.JPG'` | `'.jpg'`（轉小寫） |
| `'README'` | `''`（沒有副檔名） |
| `'archive.tar.gz'` | `'.gz'`（只取最後一個 `.`） |

#### 任務三 `parseFileMetadata`

假裝一個 formidable 的 file 物件：

```js
{ originalFilename: 'leo.jpg', size: 250000 }
```

預期回傳：

- `filename`：`'leo.jpg'`
- `sizeKB`：`Math.round(250000 / 1024)`（四捨五入）
- `ext`：呼叫任務二，得到 `'.jpg'`

另外還會測四捨五入：

| size（bytes） | sizeKB |
|---|---|
| 1024 | 1 |
| 1536 | 2 |
| 512 | 1 |

#### 任務四 `formatUploadLog`

測字串裡有沒有這些內容：健身房名、檔名、KB、上傳目錄。

格式要長這樣：

```text
[FitClub] Uploaded leo.jpg (245 KB) → /tmp/uploads
```

測試用 `toContain`，所以關鍵字對就過。  
但建議完全照這個格式寫，之後自己看 log 也比較清楚。

---

### 第 5 步：任務六是「API 測試」（用 supertest 打 HTTP）

這組不是直接呼叫函式，而是：

1. `createUploadServer(config)` 做出 server（**不要** `listen`）
2. 用 `request(server)` 假裝瀏覽器送請求
3. 看 status code 和 JSON body

例如上傳成功：

```js
const server = createUploadServer(baseConfig);
const res = await request(server)
  .post('/coaches/avatar')
  .attach('file', SAMPLE_JPG);

expect(res.status).toBe(200);
expect(res.body.filename).toBe('sample.jpg');
```

`.attach('file', SAMPLE_JPG)` 的意思是：

- 表單欄位名必須叫 `file`
- 檔案是 `fixtures/sample.jpg`

這 5 筆分別在考：

| 請求 | 預期 |
|---|---|
| `GET /` | 404，body 要有 `error` |
| `POST /unknown` | 404 |
| `POST /coaches/avatar` + 附檔 | 200，回 `filename / sizeKB / ext / savedPath` |
| 檔案超過 `maxFileSize` | 500，body 要有 `error` |
| multipart 但沒有 `file` 欄位 | 400，`error` 必須是 `'No file uploaded'` |

任務五 `router` 沒有獨立測試，但任務六會經過它，所以 `router` 也一定要寫對。

---

### 第 6 步：執行自動測試

先完成 `index.js` 的函式，再跑：

```bash
npm test
```

成功會長得像：

```text
PASS  ./test.js
  任務一：getUploadConfig
    ✓ env 都有設時回傳正確值
    ✓ env 都沒設時回傳預設值
  ...
Tests:       14 passed, 14 total
```

失敗時看這三個就夠：

1. 哪個 `test('...')` 紅了
2. `Expected`：測試要什麼
3. `Received`：你的函式實際回了什麼

對照改 `index.js`，再跑一次，直到 14 筆全過。

測試結果條列：

- ✓ 表示測試通過
- ✕ 表示測試失敗

測試項目（共 14 項）：

| 群組 | 測試數量 |
|---|---|
| 任務一：getUploadConfig | 2 |
| 任務二：getFileExtension | 4 |
| 任務三：parseFileMetadata | 2 |
| 任務四：formatUploadLog | 1 |
| 任務六：createUploadServer | 5 |

---

### 第 7 步：手動驗證（可選，但很有感覺）

自動測試過了，還可以用真 server 再試一次。

1. 專案根目錄新增 `.env`（可複製 `.env.example`）
2. 啟動：

```bash
npm start
```

3. 另開一個終端機上傳：

```bash
curl -F "file=@fixtures/sample.jpg" http://localhost:3000/coaches/avatar
```

成功會回類似：

```json
{
  "filename": "sample.jpg",
  "sizeKB": 123,
  "ext": ".jpg",
  "savedPath": "/tmp/coach-avatars/..."
}
```

#### 用 Postman

1. Method 選 `POST`
2. URL 填 `http://localhost:3000/coaches/avatar`
3. 點 Body → 選 `form-data`
4. Key 名稱輸入 `file`，類型選 **File**（不要留 Text）
5. Value 選一張圖片
6. 按 Send

若圖片旁邊出現警示符號，可參考：[Week2 Postman 圖片出現警示符號教學](https://hackmd.io/@hexschool/H1ddhaflzg)

---

## 常見問題

**Q：curl 送檔後 response 是 `{"error": "..."}`？**  
A：可能是你用的 field name 不是 `file`。`curl -F "file=@..."` 的 `file` 是 formidable 預期的欄位名。

**Q：`files.file[0]` 還是 `files.file`？**  
A：formidable v3 預設把同名檔案包成陣列（因為 multipart 允許同 key 多檔），所以是 `files.file[0]`。

**Q：為什麼測試 timeout？**  
A：Jest 預設 timeout 5 秒。如果出現 timeout，多半是 server 沒回應（路由判斷錯、忘了 `res.end()`）。檢查每個分支都有 `res.end()`。

---

## 你現在該做的事（不是寫 test.js）

`test.js` 不用改。作業流程是：

1. 打開 `index.js`
2. 依註解依序寫完 6 個函式
3. 跑 `npm test`
4. 紅燈就對照上面那張表改函式
5. 14 筆全綠再上傳 GitHub
